{
  "name": "marketplace-demo",
  "private": true,
  "workspaces": [
    "api",
    "host",
    "ui"
  ],
  "scripts": {
 "dev": "concurrently --raw -n host,api,ui -c cyan,yellow,magenta --hide api,ui \"bun --filter host --silent dev\" \"bun --filter marketplace-api --silent dev\" \"bun --filter marketplace_ui --silent dev\"",
    "dev:ui": "concurrently --raw -n host,ui -c cyan,magenta \"USE_REMOTE_API=true bun --filter host --silent dev\" \"bun --filter marketplace_ui --silent dev\"",
    "dev:api": "concurrently --raw -n host,api -c cyan,yellow \"USE_REMOTE_UI=true bun --filter host --silent dev\" \"bun --filter marketplace-api --silent dev\"",
    "preview": "bun --filter host --silent preview",
    "build": "bun --filter '*' build",
    "build:api": "bun --filter marketplace-api build",
    "build:host": "bun --filter host build",
    "build:ui": "bun --filter marketplace_ui build",
    "deploy": "bun build:ui && bun build:api",
    "typecheck": "bun --filter '*' --silent typecheck",
    "db:push": "bun --filter '{marketplace-api,host}' db:push",
    "db:studio": "bun --filter '{marketplace-api,host}' db:studio",
    "db:generate": "bun --filter '{marketplace-api,host}' db:generate",
    "db:migrate": "bun --filter '{marketplace-api,host}' db:migrate"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "packageManager": "bun@1.2.20"
}
